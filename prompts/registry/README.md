# Prompt Registry (Produção)

Prompts verificados e prontos para uso por agentes.

## Arquivos

| Arquivo | Domínio | Descrição |
|---------|---------|-----------|
| `project.yaml` | Projeto | Prompts para desenvolvimento do dare-framework (rotas, nav, packs, deploy) |

## Prompts por nível DARE

Os packs em `client/src/packs/level-[n]/prompts/registry.yaml` são os registries oficiais por nível. Para centralizar exploração/candidates de níveis aqui, use:

- `level-1.yaml` (futuro)
- `level-2.yaml` (futuro)
- `level-3.yaml` (futuro)

Sincronize com os packs quando promover.
